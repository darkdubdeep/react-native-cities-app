import { React } from 'react';
import { View, Text } from 'react-native';

const PageOne = () => {
    return (
        <View>
            <Text>Page one</Text>
        </View>
    );
};

export default PageOne;
